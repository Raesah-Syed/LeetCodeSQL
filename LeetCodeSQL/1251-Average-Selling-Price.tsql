SELECT product_id, coalesce(round(sum(n)*1.00/sum(d),2),0) as average_price from(
SELECT p.product_id, p.price*u.units as n, units as d 
from Prices p left join UnitsSold u on p.product_id=u.product_id and u.purchase_date between p.start_date and p.end_date)x
group by product_id