SELECT min(p.product_name) as product_name, sum(unit) as unit from Orders o
join Products p on p.product_id=o.product_id
where format(order_date,'yyyy-MM')='2020-02'
group by p.product_id
having sum(unit)>=100