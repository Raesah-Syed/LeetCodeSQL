/* Write your T-SQL query statement below */
SELECT product_name, unit from(
SELECT o.product_id,p.product_name, sum(unit) as unit from Products p join Orders o on p.product_id=o.product_id where order_date like '2020-02-%'
group by o.product_id,p.product_name
) x where unit>=100
